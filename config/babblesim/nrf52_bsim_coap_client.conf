# BabbleSim OpenThread: CoAP client extra settings (chain after nrf52_bsim_coap.conf).

CONFIG_COAP=y
CONFIG_COAP_UTILS=y
CONFIG_NETWORKING=y

# CoAP client prj.conf enables FPU; native bsim has no FP ABI for OT prebuilt libs.
CONFIG_FPU=n
