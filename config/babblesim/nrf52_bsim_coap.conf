# BabbleSim OpenThread: CoAP server scenario (delta on nrf52_bsim_openthread.conf).

CONFIG_OPENTHREAD_COAP=y

# CoAP samples enable FPU in prj.conf; native bsim has no FP ABI for OT prebuilt libs.
CONFIG_FPU=n
